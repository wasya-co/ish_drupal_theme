
{%
  set classes = [
    'region',
    'region-' ~ region|clean_class,
    'maxwidth'
  ]
%}
{% if content %}
  <div class='flex-column' >
    <div{{ attributes.addClass(classes) }}>
      {{ content }}
    </div>
  </div>
{% endif %}
