
{%
  set classes = [
    'region',
    'region-' ~ region|clean_class,
    'maxwidth'
  ]
%}
{% if content %}
  <div class='flex-column region-headerW' >
    <div{{ attributes.addClass(classes) }}>
      {{ content }}
    </div>
  </div>
{% endif %}
